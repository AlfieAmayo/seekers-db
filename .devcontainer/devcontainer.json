{
  "postCreateCommand": "sudo apt-get install -y postgresql && sudo service postgresql start && sudo sed -i 's/peer/trust/g' /etc/postgresql/16/main/pg_hba.conf && sudo service postgresql restart && psql -U postgres -c 'CREATE DATABASE seekers;' && for f in /workspaces/seekers-db/migrations/*.sql; do psql -U postgres -d seekers -f \"$f\"; done"
}
